export interface OrganisationModel {
    id: number;               // Identifiant auto-incrémenté
    id_organisation: string;  // Identifiant de l'organisation
    libelle: string;          // Libellé de l'organisation
  }
  